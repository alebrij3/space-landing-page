const Nav = () => {
    const view = `
        <nav>
            <a href="#">Home</a>
            <a href="#">Planets</a>
            <a href="#">Blog</a>
            <a href="#">About</a>
        </nav>
    `;
    return view;
};

export default Nav;
